/**
 * 
 */
var loseState = {
	create : function() {

		/*
		 * var winLabel = game.add.text(0, 80, 'LoseState: Press W to restart', {
		 * font : '15px Arial', fill : '#00FF00' });
		 * 
		 * var wkey = game.input.keyboard.addKey(Phaser.Keyboard.W);
		 * wkey.onDown.addOnce(this.restart, this);
		 */
	},
/*
 * restart : function() { game.state.start('menu'); },
 */

};
